# Dispatch Web

Front-end do Dispatch. Documentação de arquitetura, decisões e estado atual em `CLAUDE.md`.

```
npm run dev      # sobe o Vite dev server (porta 5173)
npm run build    # tsc -b && vite build
```
